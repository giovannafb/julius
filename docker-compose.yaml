services:
  db:
    image: postgres:15.3
    volumes:
      - ./volumes/postgres/data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: caju
      POSTGRES_USER: caju
      POSTGRES_DB: caju
    ports:
      - "5432:5432"